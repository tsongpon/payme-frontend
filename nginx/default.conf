server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Let the SPA handle routing (otherwise deep-links 404)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Enable Brotli / gzip automatically (Alpine Nginx has them compiled in)
}